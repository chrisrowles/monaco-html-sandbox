extends layout

block content
    .backdrop.d-none
    .wrapper(x-data='manager()')
        include sidebar
        #content(x-ref='content')
            include tabs
            .py-2.bg-dark
            include sandbox
        #linked(x-ref='link')=identifier
    include shareable
